title: AutoID Test

pages:
  template: autoidtest

sections:
  # -----------------------------
  images:
    extends: sections/autoidimages
    template: image

  # -----------------------------
  content:
    type: fields
    fields:
      text:
        label: Text
        type: textarea
        size: large
      autoid:
        type: hidden
      anystructure:
        label: Structure
        type: structure
        fields:
          text:
            type: text
          autoid:
            type: hidden

